Initialize an empty TODO list
  list = List.new

Add a task to a TODO list
  list.add(Task.new("string"))

Get all the tasks on a TODO list
  tasks = list.tasks

Delete a particular task from a TODO list
  list.delete(task[index])

Complete a particular task on a TODO list
  list.complete(task[index])

Display list to user
  puts list

Parse the command-line arguments and take the appropriate action
  in view class, take argument and feed to controller file
  ARGV[0]

Add function to interpret user arguments as a function
  in controller class, interpret argument as one of the functions
  call_function = list.ARGV[0] p list

Parse the todo.csv file and wrap each entry in easier-to-manipulate
Ruby objects
  CSV.foreach(read in file)



